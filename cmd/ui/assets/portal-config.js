// Development-safe portal configuration. Production replaces this generated
// asset during deployment; the tenant is provisioning data, never a URL value.
window.NETCORE_PORTAL_CONFIG = Object.freeze({
  mode: "preview",
  tenant: "",
  apiBase: ""
});
